/**
 * 미션 관련 유틸리티
 *
 * 주의: localStorage 기반 미션 완료 추적은 제거되었습니다.
 * 미션 완료 여부는 서버의 userSubmissions에서 userName과 isSubmitted로만 판단합니다.
 */

// 이 파일은 하위 호환성을 위해 유지되지만, 실제 기능은 제거되었습니다.
// 모든 미션 완료 로직은 서버 응답(userName === loginId && isSubmitted)으로 처리됩니다.
